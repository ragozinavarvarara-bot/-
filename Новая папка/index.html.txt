<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Андрей & Варвара — Wedding Invitation</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    :root{
      --forest:#355344;
      --sage:#91a48f;
      --wood:#8b6a4e;
      --linen:#f4efe7;
      --paper:#fbf8f2;
      --text:#2b2d2a;
      --muted:#676d67;
      --line:rgba(53,83,68,.12);
      --shadow:0 20px 60px rgba(30,40,30,.10);
      --radius:28px;
    }

    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
    }

    html{
      scroll-behavior:smooth;
    }

    body{
      font-family:'Inter',sans-serif;
      background:
        radial-gradient(circle at top left, rgba(145,164,143,.15), transparent 30%),
        radial-gradient(circle at top right, rgba(139,106,78,.12), transparent 25%),
        linear-gradient(180deg,#f7f2eb 0%,#f0e8dd 100%);
      color:var(--text);
      overflow-x:hidden;
      position:relative;
    }

    body::before{
      content:"";
      position:fixed;
      inset:0;
      pointer-events:none;
      opacity:.07;
      background-image:
        linear-gradient(rgba(53,83,68,.05) 1px, transparent 1px),
        linear-gradient(90deg, rgba(53,83,68,.04) 1px, transparent 1px);
      background-size:120px 120px;
      z-index:0;
    }

    .particles{
      position:fixed;
      inset:0;
      overflow:hidden;
      pointer-events:none;
      z-index:0;
    }

    .particles span{
      position:absolute;
      width:6px;
      height:6px;
      background:rgba(255,255,255,.25);
      border-radius:50%;
      animation:float 18s linear infinite;
    }

    @keyframes float{
      from{
        transform:translateY(100vh) scale(0);
        opacity:0;
      }
      20%{
        opacity:1;
      }
      to{
        transform:translateY(-10vh) scale(1);
        opacity:0;
      }
    }

    .wrap{
      width:min(1180px, calc(100% - 32px));
      margin:auto;
      position:relative;
      z-index:2;
    }

    section{
      padding:100px 0;
    }

    .hero{
      min-height:100vh;
      display:flex;
      align-items:center;
      position:relative;
    }

    .hero-grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:40px;
      align-items:center;
    }

    .glass{
      background:rgba(255,255,255,.52);
      backdrop-filter:blur(12px);
      border:1px solid rgba(255,255,255,.4);
      box-shadow:var(--shadow);
    }

    .hero-card{
      border-radius:40px;
      padding:50px;
      position:relative;
      overflow:hidden;
    }

    .hero-card::before{
      content:"";
      position:absolute;
      width:400px;
      height:400px;
      background:radial-gradient(circle, rgba(145,164,143,.18), transparent 70%);
      top:-180px;
      right:-180px;
    }

    .eyebrow{
      font-size:.75rem;
      text-transform:uppercase;
      letter-spacing:.3em;
      color:var(--wood);
      margin-bottom:18px;
    }

    h1,h2,h3{
      font-family:'Cormorant Garamond',serif;
      font-weight:500;
      line-height:1;
    }

    h1{
      font-size:clamp(4rem,8vw,7rem);
      color:var(--forest);
    }

    .amp{
      color:var(--wood);
    }

    .hero-text{
      margin-top:22px;
      font-size:1.1rem;
      line-height:1.8;
      max-width:520px;
      color:#444;
    }

    .guest{
      margin-top:30px;
      display:inline-flex;
      align-items:center;
      gap:10px;
      padding:14px 18px;
      border-radius:999px;
      background:rgba(145,164,143,.12);
      border:1px solid rgba(53,83,68,.1);
      font-size:.95rem;
    }

    .meta{
      display:flex;
      flex-wrap:wrap;
      gap:12px;
      margin-top:30px;
    }

    .meta-item{
      padding:14px 18px;
      border-radius:18px;
      background:rgba(255,255,255,.7);
      border:1px solid var(--line);
      box-shadow:0 10px 30px rgba(0,0,0,.05);
    }

    .buttons{
      display:flex;
      gap:14px;
      flex-wrap:wrap;
      margin-top:34px;
    }

    .btn{
      padding:15px 22px;
      border-radius:999px;
      text-decoration:none;
      color:white;
      font-weight:600;
      transition:.35s;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:10px;
    }

    .btn:hover{
      transform:translateY(-3px);
    }

    .btn-primary{
      background:var(--forest);
      box-shadow:0 14px 30px rgba(53,83,68,.22);
    }

    .btn-secondary{
      background:rgba(255,255,255,.7);
      color:var(--forest);
      border:1px solid var(--line);
    }

    .hero-image{
      height:760px;
      border-radius:40px;
      overflow:hidden;
      position:relative;
      box-shadow:var(--shadow);
    }

    .hero-image img{
      width:100%;
      height:100%;
      object-fit:cover;
      transform:scale(1.03);
    }

    .hero-image::after{
      content:"";
      position:absolute;
      inset:0;
      background:
        linear-gradient(to top, rgba(0,0,0,.38), transparent 45%),
        radial-gradient(circle at top, rgba(255,255,255,.15), transparent 40%);
    }

    .hero-badge{
      position:absolute;
      left:20px;
      bottom:20px;
      z-index:3;
      padding:24px;
      border-radius:24px;
      max-width:320px;
      color:white;
      background:rgba(20,20,20,.25);
      backdrop-filter:blur(10px);
      border:1px solid rgba(255,255,255,.15);
    }

    .hero-badge h3{
      font-size:2rem;
      margin-bottom:10px;
    }

    .hero-badge p{
      line-height:1.6;
      opacity:.92;
    }

    .countdown{
      display:flex;
      gap:14px;
      margin-top:34px;
      flex-wrap:wrap;
    }

    .clock{
      min-width:110px;
      padding:18px;
      border-radius:22px;
      text-align:center;
      background:rgba(255,255,255,.7);
      border:1px solid var(--line);
    }

    .clock strong{
      display:block;
      font-size:2rem;
      color:var(--forest);
      font-family:'Cormorant Garamond',serif;
    }

    .clock span{
      font-size:.85rem;
      color:#666;
    }

    .section-head{
      text-align:center;
      margin-bottom:60px;
    }

    .section-head p{
      text-transform:uppercase;
      letter-spacing:.25em;
      color:var(--wood);
      font-size:.75rem;
      margin-bottom:16px;
    }

    .section-head h2{
      font-size:clamp(2.8rem,5vw,4.5rem);
      color:var(--forest);
      margin-bottom:18px;
    }

    .section-head span{
      max-width:700px;
      display:block;
      margin:auto;
      line-height:1.8;
      color:#555;
    }

    .gallery{
      display:grid;
      grid-template-columns:repeat(2,1fr);
      gap:28px;
    }

    .photo-card{
      overflow:hidden;
      border-radius:32px;
      background:rgba(255,255,255,.55);
      border:1px solid var(--line);
      transition:.5s;
      box-shadow:var(--shadow);
    }

    .photo-card:hover{
      transform:translateY(-8px);
    }

    .photo-card img{
      width:100%;
      height:520px;
      object-fit:cover;
      transition:1s;
    }

    .photo-card:hover img{
      transform:scale(1.05);
    }

    .photo-content{
      padding:26px;
    }

    .photo-content h3{
      font-size:2rem;
      margin-bottom:10px;
      color:var(--forest);
    }

    .photo-content p{
      color:#555;
      line-height:1.7;
    }

    .timeline{
      display:grid;
      gap:20px;
    }

    .timeline-item{
      display:grid;
      grid-template-columns:140px 1fr;
      gap:20px;
      padding:24px;
      border-radius:30px;
      background:rgba(255,255,255,.65);
      border:1px solid var(--line);
      transition:.35s;
      box-shadow:var(--shadow);
    }

    .timeline-item:hover{
      transform:translateY(-4px);
    }

    .time{
      font-size:2rem;
      font-family:'Cormorant Garamond',serif;
      color:var(--wood);
    }

    .timeline-item h3{
      font-size:2rem;
      color:var(--forest);
      margin-bottom:10px;
    }

    .timeline-item p{
      color:#555;
      line-height:1.8;
    }

    .dress-grid{
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:24px;
    }

    .dress-card{
      padding:34px;
      border-radius:32px;
      background:rgba(255,255,255,.6);
      border:1px solid var(--line);
      box-shadow:var(--shadow);
    }

    .dress-card h3{
      font-size:2rem;
      margin-bottom:16px;
      color:var(--forest);
    }

    .dress-card p{
      line-height:1.8;
      color:#555;
      margin-bottom:24px;
    }

    .swatches{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
    }

    .swatch{
      width:58px;
      height:58px;
      border-radius:50%;
      border:2px solid rgba(255,255,255,.6);
      box-shadow:0 10px 24px rgba(0,0,0,.08);
    }

    .location{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:30px;
      align-items:stretch;
    }

    .location-card{
      padding:40px;
      border-radius:34px;
      background:rgba(255,255,255,.6);
      border:1px solid var(--line);
      box-shadow:var(--shadow);
    }

    .location-card h2{
      font-size:3rem;
      color:var(--forest);
      margin-bottom:18px;
    }

    .location-card p{
      line-height:1.9;
      color:#555;
      margin-bottom:24px;
    }

    iframe{
      width:100%;
      height:100%;
      min-height:480px;
      border:0;
      border-radius:34px;
      overflow:hidden;
      box-shadow:var(--shadow);
    }

    .rsvp-grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:30px;
    }

    form{
      padding:40px;
      border-radius:34px;
      background:rgba(255,255,255,.65);
      border:1px solid var(--line);
      box-shadow:var(--shadow);
    }

    input,
    textarea,
    select{
      width:100%;
      margin-top:8px;
      margin-bottom:18px;
      padding:16px;
      border-radius:18px;
      border:1px solid rgba(0,0,0,.08);
      background:rgba(255,255,255,.8);
      font:inherit;
    }

    textarea{
      min-height:140px;
      resize:none;
    }

    .submit{
      width:100%;
      border:none;
      cursor:pointer;
    }

    .success{
      margin-top:16px;
      color:var(--forest);
    }

    footer{
      padding:80px 0 60px;
      text-align:center;
      color:#666;
      position:relative;
    }

    .footer-ornament{
      margin-bottom:26px;
    }

    .fade{
      opacity:0;
      transform:translateY(40px);
      transition:1s;
    }

    .fade.show{
      opacity:1;
      transform:none;
    }

    @media(max-width:980px){

      .hero-grid,
      .gallery,
      .dress-grid,
      .location,
      .rsvp-grid{
        grid-template-columns:1fr;
      }

      .hero-image{
        height:580px;
      }
    }

    @media(max-width:640px){

      section{
        padding:70px 0;
      }

      .hero-card{
        padding:28px;
      }

      h1{
        font-size:4rem;
      }

      .hero-image{
        height:440px;
      }

      .timeline-item{
        grid-template-columns:1fr;
      }

      .clock{
        flex:1 1 45%;
      }

      iframe{
        min-height:320px;
      }
    }
  </style>
</head>
<body>

<div class="particles">
  <span style="left:10%; animation-delay:0s;"></span>
  <span style="left:20%; animation-delay:3s;"></span>
  <span style="left:35%; animation-delay:6s;"></span>
  <span style="left:50%; animation-delay:1s;"></span>
  <span style="left:65%; animation-delay:7s;"></span>
  <span style="left:80%; animation-delay:4s;"></span>
  <span style="left:90%; animation-delay:9s;"></span>
</div>

<section class="hero" id="top">
  <div class="wrap hero-grid">

    <div class="hero-card glass fade">

      <div class="eyebrow">Wedding Invitation</div>

      <h1>
        Андрей <span class="amp">&</span> Варвара
      </h1>

      <p class="hero-text">
        Будем счастливы разделить с вами наш самый тёплый и важный день.
      </p>

      <div class="guest">
        Для: <strong id="guestName">Дорогие друзья</strong>
      </div>

      <div class="meta">
        <div class="meta-item">🌿 21.08.2026</div>
        <div class="meta-item">🕰 16:30</div>
        <div class="meta-item">📍 Лесной</div>
      </div>

      <div class="buttons">
        <a href="#rsvp" class="btn btn-primary">Подтвердить</a>
        <a href="https://go.2gis.com/lIPNn" target="_blank" class="btn btn-secondary">Открыть карту</a>
      </div>

      <div class="countdown">
        <div class="clock">
          <strong id="days">00</strong>
          <span>дней</span>
        </div>

        <div class="clock">
          <strong id="hours">00</strong>
          <span>часов</span>
        </div>

        <div class="clock">
          <strong id="minutes">00</strong>
          <span>минут</span>
        </div>

        <div class="clock">
          <strong id="seconds">00</strong>
          <span>секунд</span>
        </div>
      </div>

    </div>

    <div class="hero-image fade">
      <img src="image(1).png" alt="Андрей и Варвара">

      <div class="hero-badge">
        <h3>21 августа</h3>
        <p>
          Загородный вечер среди леса, тёплого света и самых близких людей.
        </p>
      </div>
    </div>

  </div>
</section>

<section>
  <div class="wrap">

    <div class="section-head fade">
      <p>Love Story</p>
      <h2>Наша история</h2>
      <span>
        Немного настоящих моментов, которые хочется сохранить навсегда.
      </span>
    </div>

    <div class="gallery">

      <div class="photo-card fade">
        <img src="image(1).png" alt="">
        <div class="photo-content">
          <h3>Вершины</h3>
          <p>
            Самые важные моменты всегда происходят там, где спокойно сердцу.
          </p>
        </div>
      </div>

      <div class="photo-card fade">
        <img src="image(1).png" alt="">
        <div class="photo-content">
          <h3>Тепло</h3>
          <p>
            Эта история — про нас, про любовь и про людей рядом.
          </p>
        </div>
      </div>

    </div>

  </div>
</section>

<section>
  <div class="wrap">

    <div class="section-head fade">
      <p>Wedding Day</p>
      <h2>Программа вечера</h2>
    </div>

    <div class="timeline">

      <div class="timeline-item fade">
        <div class="time">16:30</div>
        <div>
          <h3>Сбор гостей</h3>
          <p>
            Объятия, фотографии и первые тёплые встречи.
          </p>
        </div>
      </div>

      <div class="timeline-item fade">
        <div class="time">17:00</div>
        <div>
          <h3>Церемония</h3>
          <p>
            Самый важный момент нашего дня.
          </p>
        </div>
      </div>

      <div class="timeline-item fade">
        <div class="time">18:00</div>
        <div>
          <h3>Ужин</h3>
          <p>
            Душевный вечер, музыка и много улыбок.
          </p>
        </div>
      </div>

      <div class="timeline-item fade">
        <div class="time">21:00</div>
        <div>
          <h3>Торт и танцы</h3>
          <p>
            Финал вечера под огнями и музыку.
          </p>
        </div>
      </div>

    </div>

  </div>
</section>

<section>

  <div class="wrap">

    <div class="section-head fade">
      <p>Dress Code</p>
      <h2>Палитра вечера</h2>
    </div>

    <div class="dress-grid">

      <div class="dress-card fade">
        <h3>Для девушек</h3>
        <p>
          Sage, бежевый, молочный, лен, природные оттенки.
        </p>

        <div class="swatches">
          <div class="swatch" style="background:#d8c7b1"></div>
          <div class="swatch" style="background:#91a48f"></div>
          <div class="swatch" style="background:#ede3d6"></div>
          <div class="swatch" style="background:#6f846a"></div>
        </div>
      </div>

      <div class="dress-card fade">
        <h3>Для мужчин</h3>
        <p>
          Графит, forest green, wood tones и спокойные ткани.
        </p>

        <div class="swatches">
          <div class="swatch" style="background:#44484a"></div>
          <div class="swatch" style="background:#355344"></div>
          <div class="swatch" style="background:#8b6a4e"></div>
          <div class="swatch" style="background:#1e1e1e"></div>
        </div>
      </div>

      <div class="dress-card fade">
        <h3>Общая эстетика</h3>
        <p>
          Rustic, forest wedding, linen textures и cinematic mood.
        </p>

        <div class="swatches">
          <div class="swatch" style="background:#355344"></div>
          <div class="swatch" style="background:#91a48f"></div>
          <div class="swatch" style="background:#8b6a4e"></div>
          <div class="swatch" style="background:#f4efe7"></div>
        </div>
      </div>

    </div>

  </div>

</section>

<section>

  <div class="wrap location">

    <div class="location-card fade">
      <div class="eyebrow">Location</div>

      <h2>Лесной</h2>

      <p>
        Загородный вечер среди леса, дерева и тёплого света.
      </p>

      <a href="https://go.2gis.com/lIPNn" target="_blank" class="btn btn-primary">
        Открыть карту
      </a>
    </div>

    <div class="fade">
      <iframe
        src="https://maps.google.com/maps?q=55.160226,61.319575&z=15&output=embed">
      </iframe>
    </div>

  </div>

</section>

<section id="rsvp">

  <div class="wrap">

    <div class="section-head fade">
      <p>RSVP</p>
      <h2>Подтвердите присутствие</h2>
    </div>

    <div class="rsvp-grid">

      <form id="form" class="fade">

        <input type="text" placeholder="Ваше имя" required>

        <input type="text" placeholder="Телефон">

        <select>
          <option>1 гость</option>
          <option>2 гостя</option>
          <option>3 гостя</option>
          <option>4+ гостей</option>
        </select>

        <textarea placeholder="Пожелания или комментарий"></textarea>

        <button class="btn btn-primary submit">
          Отправить ответ
        </button>

        <div class="success" id="success"></div>

      </form>

      <div class="location-card fade">
        <div class="eyebrow">Warm words</div>

        <h2>
          Ваше присутствие — лучший подарок
        </h2>

        <p>
          Мы мечтаем о вечере, наполненном теплом, смехом и самыми близкими людьми рядом.
        </p>

        <p>
          Будем счастливы разделить этот день вместе с вами.
        </p>
      </div>

    </div>

  </div>

</section>

<footer>

  <div class="footer-ornament">
    ✦ ✦ ✦
  </div>

  Андрей & Варвара — 21 августа 2026

</footer>

<script>

  // guest personalization

  const params = new URLSearchParams(window.location.search);
  const guest = params.get('guest');

  if(guest){
    document.getElementById('guestName').textContent = guest;
  }

  // countdown

  const weddingDate = new Date('2026-08-21T16:30:00+05:00').getTime();

  function updateCountdown(){

    const now = new Date().getTime();
    const distance = weddingDate - now;

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    document.getElementById('days').innerHTML = days;
    document.getElementById('hours').innerHTML = hours;
    document.getElementById('minutes').innerHTML = minutes;
    document.getElementById('seconds').innerHTML = seconds;
  }

  setInterval(updateCountdown,1000);
  updateCountdown();

  // form

  const form = document.getElementById('form');
  const success = document.getElementById('success');

  form.addEventListener('submit', function(e){
    e.preventDefault();

    success.innerHTML = 'Спасибо! Ваш ответ сохранён 💛';

    form.reset();

    setTimeout(()=>{
      success.innerHTML = '';
    },5000)
  });

  // fade animations

  const observer = new IntersectionObserver((entries)=>{
    entries.forEach(entry=>{
      if(entry.isIntersecting){
        entry.target.classList.add('show');
      }
    });
  }, {threshold:.15});

  document.querySelectorAll('.fade').forEach(el=>{
    observer.observe(el);
  });

</script>

</body>
</html>